#include<bits/stdc++.h>
using namespace std;

int main()
{
    string str, str2; 
    str = "Hello"; //We need to use double quotation for string. 
    cout<<str<<endl;
    cin>>str2; //If the input is: World Is Beautiful
    cout<<str2<<endl; // it returns "World"
}