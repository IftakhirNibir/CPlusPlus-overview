#include<bits/stdc++.h>
using namespace std;

int main() {
   char c = 'a';
   cout <<(int) c <<endl;//convert char to integer(ASCII value) so the output is 97
   char d = 'b';
   cout << d-c; //it returns 1 because ASCII b=98 and a=97 
}